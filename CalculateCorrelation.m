function correlation = CalculateCorrelation(dataset1, dataset2)
    % TODO:
    % - fix corellation calculation
    % - filter out the countries which do not occur in both datasets
    % (UnifyDatasets.m)

%     correlation = corrcoef(dataset1Unified(1,:), dataset2Unified(1,:), 'rows', 'complete');
%     correlation = corrcoef(dataset1Unified(1,:), dataset2Unified(1,:));
end

